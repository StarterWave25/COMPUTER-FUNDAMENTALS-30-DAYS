**Okeyyyy, Okeyyyy, Now we need compress the araay**
```
1. Here you need take the sub-arrays in array (at any sizes) and find the maximum value in it and replace that entire sub-array (you have taken) with maximum value you found in the sub-array.

2. At every operation (means replacing that subarray with maximum value) will cost length of subarray x maximum value in subarray.

3. So, For findnig the best and optimal solution i need to compress the array with low operation cost.

4. For that i will sort the array first. And then take whole array as sub-array.

5. So Length of array x Maximum Value in the array will be the operation which is the lowest one.

```